import { motion } from 'framer-motion'
import { ArrowLeft, ArrowRight } from 'lucide-react'
import type { ReactNode } from 'react'
import { useTranslation } from 'react-i18next'
import { Link } from 'react-router-dom'
import { AuthCard } from './AuthCard'
import { AuthLogo } from './AuthLogo'
export function AuthLayout({ title, description, children }: { title: string; description: string; children: ReactNode }) { const { t, i18n } = useTranslation('auth'); const Arrow = i18n.resolvedLanguage === 'ar' ? ArrowRight : ArrowLeft; return <main className="relative flex min-h-[100dvh] items-center justify-center overflow-hidden bg-[#F8FAFC] px-4 py-8 font-['Cairo'] sm:px-6"><div className="pointer-events-none absolute left-1/2 top-1/2 size-[34rem] -translate-x-1/2 -translate-y-1/2 rounded-full bg-[radial-gradient(circle,rgba(16,185,129,0.12)_0%,rgba(236,253,245,0)_62%)]" /><Link to="/" aria-label={t('actions.backHomeLabel')} className="group absolute end-4 top-4 z-10 inline-flex min-h-11 min-w-11 items-center justify-center gap-2 rounded-xl border border-slate-200 bg-white/85 px-3 text-sm font-bold text-emerald-700 shadow-sm backdrop-blur-sm hover:bg-emerald-50 focus-visible:ring-2 focus-visible:ring-emerald-700 sm:end-6 sm:top-6"><Arrow className="size-5" aria-hidden="true" /><span className="hidden sm:inline">{t('actions.backHome')}</span></Link><motion.div initial={{ opacity: 0, y: 14 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.25 }} className="relative flex w-full flex-col items-center"><div className="mb-7 text-center"><AuthLogo /><p className="mt-3 text-sm font-medium text-slate-500 sm:text-base">{description}</p></div><AuthCard><h1 className="text-start text-2xl font-extrabold tracking-[-0.02em] text-[#0F172A]">{title}</h1>{children}</AuthCard></motion.div></main> }
