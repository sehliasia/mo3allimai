import { useEffect, useState } from 'react'; import { getAdminStatistics } from '../../services/adminService'
export function AdminStatisticsPage() { const [stats, setStats] = useState<{ total_teachers: number; active_teachers: number; inactive_teachers: number; new_teachers_this_month: number } | null>(null); const load = () => void getAdminStatistics().then(setStats).catch(() => setStats(null)); useEffect(load, []); const items = [['إجمالي الأساتذة', stats?.total_teachers], ['الحسابات النشطة', stats?.active_teachers], ['الحسابات الموقوفة', stats?.inactive_teachers], ['التسجيلات الجديدة هذا الشهر', stats?.new_teachers_this_month]]; return <><h2 className="text-3xl font-extrabold">الإحصائيات</h2><div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{items.map(([l,v])=><div key={String(l)} className="rounded-2xl border bg-white p-5"><p className="text-sm text-slate-500">{l}</p><p className="mt-3 text-3xl font-extrabold">{v ?? '—'}</p></div>)}</div><p className="mt-8 rounded-xl bg-emerald-50 p-4 text-emerald-900">سيتم إضافة إحصائيات استخدام أدوات الذكاء الاصطناعي لاحقاً</p></> }
