import { FileText, Plus, Search, Upload, X } from 'lucide-react'
import { useEffect, useRef, useState } from 'react'
import { useTranslation } from 'react-i18next'

const documentTypes = ['cecrl', 'miftah', 'guide', 'program', 'other'] as const
const languages = ['ar', 'fr', 'en', 'es', 'other'] as const
const skills = ['listening', 'reading', 'speaking', 'writing', 'interaction', 'vocabulary', 'grammar', 'other'] as const

export function KnowledgeBasePage() {
  const { t } = useTranslation('admin')
  const [open, setOpen] = useState(false)
  const [file, setFile] = useState<File | null>(null)
  const [errors, setErrors] = useState<string[]>([])
  const [values, setValues] = useState({ title: '', type: '', language: '', level: 'all', skill: '', source: '', description: '' })
  const fileInput = useRef<HTMLInputElement>(null)
  useEffect(() => { const close = (event: KeyboardEvent) => event.key === 'Escape' && setOpen(false); document.addEventListener('keydown', close); return () => document.removeEventListener('keydown', close) }, [])
  const setValue = (key: keyof typeof values, value: string) => setValues(current => ({ ...current, [key]: value }))
  const selectFile = (next: File | undefined) => { setFile(next ?? null); if (next && next.type !== 'application/pdf') setErrors([t('knowledge.invalidPdf')]); else setErrors([]) }
  const validate = () => { const next = [!values.title, !values.type, !values.language, !file].some(Boolean) ? [t('knowledge.required')] : file?.type !== 'application/pdf' ? [t('knowledge.invalidPdf')] : []; setErrors(next); return next.length === 0 }
  const close = () => { setOpen(false); setErrors([]) }
  const fieldClass = 'mt-1 w-full rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-start text-sm outline-none focus:border-emerald-600 focus:ring-4 focus:ring-emerald-100'
  const languageLabel = (language: string) => language === 'other' ? t('knowledge.types.other') : t(`common:languages.${language}`)
  return <div className="space-y-6"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="text-xl font-extrabold text-slate-900">{t('page.knowledge.title')}</h2><p className="mt-1 text-sm text-slate-500">{t('page.knowledge.description')}</p></div><button type="button" onClick={() => setOpen(true)} className="inline-flex items-center gap-2 rounded-xl bg-emerald-700 px-4 py-2.5 text-sm font-bold text-white hover:bg-emerald-800 focus-visible:ring-2 focus-visible:ring-emerald-600"><Plus className="size-4" />{t('knowledge.add')}</button></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{['all', 'indexed', 'processing', 'error'].map(key => <div key={key} className="rounded-2xl border border-slate-200 bg-white p-4"><p className="text-sm text-slate-500">{t(`knowledge.${key}`)}</p><p className="mt-2 text-2xl font-extrabold text-slate-900">0</p></div>)}</div><div className="rounded-2xl border border-slate-200 bg-white p-4"><div className="grid gap-3 md:grid-cols-4"><label className="relative md:col-span-2"><Search className="pointer-events-none absolute start-3 top-1/2 size-4 -translate-y-1/2 text-slate-400" /><input aria-label={t('knowledge.search')} placeholder={t('knowledge.search')} className="w-full rounded-xl border border-slate-200 py-2.5 ps-10 pe-3 text-sm focus:border-emerald-600 focus:outline-none" /></label><select aria-label={t('knowledge.type')} className="rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-sm"><option>{t('knowledge.type')}</option>{documentTypes.map(option => <option key={option}>{t(`knowledge.types.${option}`)}</option>)}</select><select aria-label={t('knowledge.language')} className="rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-sm"><option>{t('knowledge.language')}</option>{languages.map(option => <option key={option}>{languageLabel(option)}</option>)}</select></div></div><div className="rounded-2xl border border-slate-200 bg-white px-5 py-14 text-center"><FileText className="mx-auto size-10 text-emerald-700" /><h3 className="mt-4 font-extrabold text-slate-900">{t('knowledge.emptyTitle')}</h3><p className="mx-auto mt-2 max-w-lg text-sm leading-6 text-slate-500">{t('knowledge.emptyDescription')}</p><button type="button" onClick={() => setOpen(true)} className="mt-5 inline-flex items-center gap-2 rounded-xl border border-emerald-200 px-4 py-2.5 text-sm font-bold text-emerald-700 hover:bg-emerald-50"><Plus className="size-4" />{t('knowledge.add')}</button></div>{open && <div role="dialog" aria-modal="true" aria-label={t('knowledge.add')} className="fixed inset-0 z-[60] flex items-center justify-center bg-slate-950/40 p-4"><div className="max-h-[90vh] w-full max-w-2xl overflow-y-auto rounded-2xl bg-white p-6 shadow-2xl"><div className="flex items-center justify-between"><h2 className="text-lg font-extrabold">{t('knowledge.add')}</h2><button type="button" onClick={close} aria-label="Close" className="rounded-lg p-2 hover:bg-slate-100"><X className="size-5" /></button></div>{errors.map(error => <p key={error} role="alert" className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>)}<div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="sm:col-span-2 text-sm font-semibold">{t('knowledge.title')}<input value={values.title} onChange={event => setValue('title', event.target.value)} className={fieldClass} /></label><label className="text-sm font-semibold">{t('knowledge.type')}<select value={values.type} onChange={event => setValue('type', event.target.value)} className={fieldClass}><option value="">—</option>{documentTypes.map(type => <option key={type} value={type}>{t(`knowledge.types.${type}`)}</option>)}</select></label><label className="text-sm font-semibold">{t('knowledge.language')}<select value={values.language} onChange={event => setValue('language', event.target.value)} className={fieldClass}><option value="">—</option>{languages.map(language => <option key={language} value={language}>{languageLabel(language)}</option>)}</select></label><label className="text-sm font-semibold">{t('knowledge.level')}<select value={values.level} onChange={event => setValue('level', event.target.value)} className={fieldClass}>{['all', 'A1', 'A2', 'B1', 'B2', 'C1', 'C2'].map(level => <option key={level}>{level === 'all' ? t('knowledge.all') : level}</option>)}</select></label><label className="text-sm font-semibold">{t('knowledge.skill')}<select value={values.skill} onChange={event => setValue('skill', event.target.value)} className={fieldClass}><option value="">—</option>{skills.map(skill => <option key={skill}>{t(`knowledge.skills.${skill}`)}</option>)}</select></label><label className="sm:col-span-2 text-sm font-semibold">{t('knowledge.source')}<input value={values.source} onChange={event => setValue('source', event.target.value)} className={fieldClass} /></label><label className="sm:col-span-2 text-sm font-semibold">{t('knowledge.description')}<textarea value={values.description} onChange={event => setValue('description', event.target.value)} className={fieldClass} rows={3} /></label><div className="sm:col-span-2"><p className="text-sm font-semibold">{t('knowledge.file')}</p><input ref={fileInput} type="file" accept="application/pdf" className="sr-only" onChange={event => selectFile(event.target.files?.[0])} /><button type="button" onClick={() => fileInput.current?.click()} className="mt-2 flex w-full flex-col items-center rounded-xl border-2 border-dashed border-slate-200 px-5 py-7 text-sm hover:border-emerald-400"><Upload className="size-6 text-emerald-700" /><span className="mt-2 font-bold">{file?.name ?? t('knowledge.dropzone')}</span><span className="mt-1 text-slate-500">{t('knowledge.browse')} · {t('knowledge.pdfOnly')}</span></button></div></div><div className="mt-5 flex justify-end gap-3"><button type="button" onClick={close} className="rounded-xl px-4 py-2.5 text-sm font-bold hover:bg-slate-100">{t('common:cancel')}</button><button type="button" onClick={validate} className="rounded-xl bg-slate-200 px-4 py-2.5 text-sm font-bold text-slate-500">{t('knowledge.servicePending')}</button></div></div></div>}</div>
}
