import { useAuth } from '../../contexts/AuthContext'
import { useTranslation } from 'react-i18next'
export function AdminSettingsPage() { const { user } = useAuth(); const { t } = useTranslation('admin'); return <><h2 className="text-start text-3xl font-extrabold">{t('page.settings.title')}</h2><section className="mt-7 max-w-xl rounded-2xl border bg-white p-6"><p>{user?.full_name}</p><p className="mt-2 text-slate-600">{user?.email}</p><p className="mt-4 text-emerald-700">{t('settings.admin')} · {t('settings.active')}</p><p className="mt-8 rounded-lg bg-slate-50 p-3 text-slate-600">{t('settings.development')}</p></section></> }
